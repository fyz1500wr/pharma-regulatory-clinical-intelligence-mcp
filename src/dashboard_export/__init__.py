"""MVP dashboard export artifact prototype package.

Fixture-only, manual-execution prototype that produces dashboard artifacts
from controlled mock data. It does not call live FDA, TFDA, or
ClinicalTrials.gov sources, does not add an MCP tool, scheduler, persistence
layer, HTTP/SSE surface, or dashboard renderer.
"""
